# 版本说明

当前文档基于 `v0.2.x` API：
- `runTask`
- `runTask`（数组入参支持并发模式）
- `createRunner`
- `useTask`（React / Vue）

如果你从更早草案升级，建议重点检查：
- Hook 参数命名是否为 `params`
- 是否从 `mangoo/react`、`mangoo/vue` 子路径导入
- `runTask` 失败模式是否符合预期
