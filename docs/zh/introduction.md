# 简介

## mangoo 解决什么问题
日常业务里，原生 `async/await` 很清晰，但通常会重复写这些逻辑：
1. `loading/error/data` 状态维护
2. 组件卸载取消请求
3. 并发任务限流
4. 异步错误格式不统一

mangoo 的定位是：保留原生写法，只抽离这些重复护栏。

## 不做什么
1. 不绑定 axios/fetch
2. 不要求后端返回固定结构
3. 不强制 DSL 编排
